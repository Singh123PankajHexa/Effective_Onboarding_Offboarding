# Server
PORT=4000

# Postgres (with pgvector extension enabled for the AI Assistant)
DATABASE_URL=postgresql://user:password@localhost:5432/onboarding_offboarding

# AI / model routing (JusCode)
JUSCODE_API_KEY=

# Notifications (fill in when implementing)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
TEAMS_WEBHOOK_URL=
